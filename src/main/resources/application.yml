# Spring properties
spring:
  application:
     name: cmpl
  datasource:
    platform: h2
    url: jdbc:h2:file:./target/cmpl
  jpa:
    hibernate.ddl-auto: update
    database: H2
  logging:
    level:
      root: INFO
      org.springframework.web: ERROR
      org.hibernate: ERROR
    pattern:
      console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
  
  social:
    facebook:
      appId: 1665545340392413
      appSecret: 490697887bcb469947a5a70ccef070c4
# HTTP Server
server:
  port: 3232  # HTTP (Tomcat) port
  
  
fileBasePath: "/home/lperrod/projets/cmpl/img/actualites/"
imageDisplaySrc: "http://cm-pl.com/"
  